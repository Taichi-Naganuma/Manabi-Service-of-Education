# Manabi 週次レポート 2026-05-25

**作成**: CEO Bob（毎週月曜日 自動生成）

---

## 今週の開発進捗

> ⚠️ 直近7日間（2026-05-18〜2026-05-25）はコミットなし。先週・先々週の主要コミットを含めた最終状況を記録。

### 直近2週間の主要コミット（参考）
- **2026-05-15** `fix(onboarding)`: CTA 3カードの色分け強弱を排し並列同色に統一（不可侵原則 #7 違反 UI 解消）
  - GoAsBoth()メソッド追加 → `/profile?initial=both` 遷移
  - ロール流動性を強調する peer voice テキストに差し替え
- **2026-05-10** `fix`: ChatHub.SendMessage でメール通知が呼ばれていなかった実装漏れを修正
  - EmailNotificationPreference チェック追加
  - try-catch で囲み、通知失敗時もメッセージ送信を継続する設計に統一

---

## タスク状況

### 完了
- ✅ **認証基盤**: JWT 認証・登録・ログイン（AuthController）
- ✅ **リアルタイムチャット**: SignalR ChatHub + DirectMessage API
- ✅ **ロールオンボーディング**: Speaker / Listener 役割選択 + 両方ロール（GoAsBoth）
- ✅ **ロールバッジ**: プロフィール・メンバーカードへの Speaker/Listener バッジ表示
- ✅ **Mattering機能群**:
  - WitnessLetter（証人の手紙）コントローラ・DB実装
  - StoryCards（ストーリーカード）管理
  - SelfReflection（内省）機能
  - SessionStart UI
- ✅ **LifeDecisions（通った岐路）**: `broke_up`（失恋）含む選択肢追加、DB Migration 適用済み
- ✅ **OGP / Twitter Card 対応**: `index.html` に meta タグ追加、`ogp.png` 同梱
- ✅ **キャッチコピー**: 「迷ったら、聞きにくる。いつか、話しにくる。」に差し替え済み
- ✅ **アカウント削除（ハイブリッド方式）**: AccountDeletionService + AccountDeletionLog
- ✅ **ユーザー操作動線ログ**: UserActionLog + AnalyticsController
- ✅ **メール通知設定**: EmailNotificationPreference モデル・設定 UI
- ✅ **本番デプロイ基盤**: Docker + Railway（SSL設定・PG環境変数・MIME type 対応済み）
- ✅ **オンボーディング UI統一**: 不可侵原則 #7（CTA カード並列同色）適用完了

### 進行中
- 🔄 **β公開後モニタリング**: UserActionLog によるユーザー動線の計測・分析
- 🔄 **UI/UX 磨き込み**: 取締役会フィードバック（#12 等）を随時反映中
- 🔄 **ロール活用機能の充実**: Speaker/Listener バッジ表示は完成、次フェーズのマッチングロジックへ展開予定

### 未着手（次の優先順）
1. **自動テスト基盤の整備**（テストプロジェクトが存在しない状態）
2. **セッションスケジューリング**（Session モデルは定義済み、予約フローが未実装）
3. **レビュー・評価システム**（Review モデル定義済み、Controller 未実装）
4. **決済・サブスクリプション機能**（現状フリーβのみ）
5. **通報・ブロック機能の UI 接続**（BlocksController・ReportsController はバックエンド実装済み）

---

## 課題・ブロッカー

| # | 課題 | 深刻度 | 状況 |
|---|------|--------|------|
| 1 | **テストがゼロ**：`Manabi.sln` にテストプロジェクトなし。リグレッション検知不能 | 🔴 高 | 未着手 |
| 2 | **Sprint 停滞**：直近7日間コミットなし。β後の疲弊・優先順位再調整が必要か | 🟡 中 | 要因調査 |
| 3 | **LifeDecisions の `broke_up` 項目**：ユーザーへの感情的センシティビティ設計（コンテンツモデレーション）未定義 | 🟡 中 | 仕様検討中 |
| 4 | **Railway デプロイの単一コンテナ構成**：API + Client を1サービスに集約 → スケールアウト時の分離コストが高い | 🟡 中 | 現状はβ規模で許容範囲 |
| 5 | **SendGrid 依存**：`IEmailService` 実装は SendGrid 固定。障害時のフォールバックなし | 🟠 低-中 | 監視強化で対応 |

---

## 来週のBobアクション予定

1. **テストプロジェクト `Manabi.Tests` を作成**し、AuthController・ChatHub の最小限ユニットテストを追加（CI 基盤整備の第一歩）
2. **Sprint 停滞の原因確認**：Taichi との 1on1 で直近1週間のブロッカーを特定、次スプリント計画を合意する
3. **セッションスケジューリング仕様書の作成**：Session モデル（`SessionStatus` 含む）を踏まえた予約フロー UX を定義
4. **通報・ブロック UI の接続**：バックエンド完成済みの BlocksController / ReportsController にフロントエンドを繋ぎ、β安全性を高める
5. **UserActionLog データ初回集計**：AnalyticsController を使い β 開始以降の動線ヒートマップを確認、次スプリントの優先順位に反映

---

## 補足：現在のアーキテクチャ概要

```
[Blazor WASM Client]
  Pages: Home / Onboarding / Teachers / Chat / Messages
       / Profile / UserProfile / MyLetters / AccountSettings
         ↓ HTTP REST + SignalR WebSocket
[ASP.NET Core 9 API]
  Controllers: Auth / Users / Teachers / Messages
             / Letters / Stories / Reflections
             / Notifications / Analytics / Blocks / Reports
  Hubs: ChatHub (SignalR)
  Services: Chat / Teacher / Token / Email(SendGrid) / AccountDeletion
         ↓ EF Core + Npgsql
[PostgreSQL on Railway]
  Migrations: 6 applied (最終: AddLifeDecisions 2026-05-07)
```

---
*このレポートはCEO Bobにより毎週月曜日09:00 JSTに自動生成されます*
